services:
  - type: web
    name: cliproxyapi
    env: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: DEPLOY
        value: cloud
      - key: MANAGEMENT_PASSWORD
        generateValue: true
